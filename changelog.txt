1.3.4 -- No Surgical pathway option

+ Added "No Surgical Pathway" option to the pre-op message replies

1.3.3 -- GUI Changes

+ Added a display of the update logs in the gui so users can see the new changes

+ Added a status bar at the bottom displaying the current version, will use this to post announcements at a later time

/ Reworked some of the update logic

# If you have any suggestions or changes you want made please do reach out!

1.3.2 -- Local Git

Added a mini git into the project files so when installing/updating on a new pc you do not need to install git.
This does make the size of the project like twice as large but this should not be too big of a problem.
If it is you can always delete "/dependencies/MinGit..." and install git as usual.

1.3.1 -- Update test

Testing if the below is true

1.3.0 -- Update update

Changed the update script to update the current script dir. this means shortcuts still work, and configs do not get reset.
Just testing now so I may need to patch it later.
Also changed some options in pre-op booking scripts.

1.2.2 -- options changes

added and changed some.

1.2.1 -- More options

more pre-op message centre options
fixes

1.2.0 -- Pre-Op Message centre addition

Now added pre-op message centre replies
Small bugfixes

1.1.7 -- Mouse adjustment

1.1.6 -- Mouse Fix

Fixed color and size

1.1.5 -- more fixes

Fixed Master opening file explorer and not the scripts
Added a test mouse guide to assist in seeing the cursor location
Changed some file names and other backend items.  

1.1.4 -- Small fixes

- Renamed "MasterV2" to just "Master"
- "Initials" now replaced with full name field, and initials are calculated automatically based on the name.
- Fixed "Not a MRN:" message when the MRN incldes spaces, searching now works with any amount of spaces included with the MRN.
- New options on Pre-Op Messages.
^ Currently I reorder the default values to whatever I most use in the moment, but ill add a way to set defaults later.

1.1.3 -- shorthand translator test --

- Started adding custom icons, so not all scripts will be a green square now. just need to find a good set of icons to use first.
- Updated GUI elements to be more consistent across scripts 
- Other small fixes/QoL
- File structure reorganisation (no user effect)
- ShorthandTrans.ahk is a script that will help translate abbreviations and shorthand back to full words. will update once finished.

1.1.2 -- clarifications --

- added the old logs to this file, so if someone skips multiple versions they can still be up-to-date.
- fixed readme outdated info and added new instructions

1.1.1 -- log fix --

- fix for that annoying error when a log folder is not found.
- if it keeps happening feel free to disable logs in the config. its optional

1.1.0 -- Text message sending test --

- Added a script to pre-fill text messages with some templates which i can add onto later.
- I should really put the two pre-op scripts in one, but the code is too messy to do that right now.
- Fix for this issue coming really soon.

1.0.0 -- THE FIRST --

- Added auto updating, which hopefully works without issue.
- Cleaned up codebase and removed redundant files.

Great things to come soon.